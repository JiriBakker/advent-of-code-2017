import { Day14 } from '../challenges/day14';
import { expect } from 'chai';

describe('Day 14', () => {
    const challenge = new Day14();

    // it('a(...)', () => {
    //     expect(challenge.a([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -14 if a >= 1",
    //         "c inc -20 if c == 14"
    //     ])).to.equal(1);
    // });

    // it('b(...)', () => {
    //     expect(challenge.b([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -14 if a >= 1",
    //         "c inc -20 if c == 14"
    //     ])).to.equal(14);
    // });
    
});

