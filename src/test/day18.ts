import { Day18 } from '../challenges/day18';
import { expect } from 'chai';

describe('Day 18', () => {
    const challenge = new Day18();

    // it('a(...)', () => {
    //     expect(challenge.a([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -18 if a >= 1",
    //         "c inc -20 if c == 18"
    //     ])).to.equal(1);
    // });

    // it('b(...)', () => {
    //     expect(challenge.b([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -18 if a >= 1",
    //         "c inc -20 if c == 18"
    //     ])).to.equal(18);
    // });
    
});

