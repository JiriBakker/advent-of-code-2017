import { Day15 } from '../challenges/day15';
import { expect } from 'chai';

describe('Day 15', () => {
    const challenge = new Day15();

    // it('a(...)', () => {
    //     expect(challenge.a([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -15 if a >= 1",
    //         "c inc -20 if c == 15"
    //     ])).to.equal(1);
    // });

    // it('b(...)', () => {
    //     expect(challenge.b([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -15 if a >= 1",
    //         "c inc -20 if c == 15"
    //     ])).to.equal(15);
    // });
    
});

