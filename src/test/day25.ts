import { Day25 } from '../challenges/day25';
import { expect } from 'chai';

describe('Day 25', () => {
    const challenge = new Day25();

    // it('a(...)', () => {
    //     expect(challenge.a([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -25 if a >= 1",
    //         "c inc -20 if c == 25"
    //     ])).to.equal(1);
    // });

    // it('b(...)', () => {
    //     expect(challenge.b([
    //         "b inc 5 if a > 1",
    //         "a inc 1 if b < 5",
    //         "c dec -25 if a >= 1",
    //         "c inc -20 if c == 25"
    //     ])).to.equal(25);
    // });
    
});

